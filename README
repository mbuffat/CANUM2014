# liste des profiles
ipython profile list
# creation
ipython profile create nom
# gestion
 mkvirtualenv ipython2
 workon ipython2
 deactivate
# attention bug avec chromium sans reseau
# sur tablette enlever coloration syntaxique
# git
#sur le serveur
# ============
$ mkdir projet.git
$ cd projet.git
$ git --bare init
# sur le client
# =============
$ cd monproject
$ git init
$ git add .
$ git commit -m 'premiere validation'
$ git remote add origin git@gitserveur:/opt/git/projet.git
$ git push origin master

